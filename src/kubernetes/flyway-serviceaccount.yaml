# Отдельный ServiceAccount только для Job миграций Flyway (не shared с приложением).
apiVersion: v1
kind: ServiceAccount
metadata:
  name: flyway-migrate
  namespace: cinemaabyss
  labels:
    app.kubernetes.io/component: database-migration
    app.kubernetes.io/name: cinemaabyss
